# A simple action usage
name: Build Kernel spes 4.19 BPF
on:
  workflow_dispatch:
  push:
    branches:
      - main-a16-sm8250-base-ksufs

jobs:
  Build-Kernel:
    strategy:
      fail-fast: false
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Build Kernel spes 4.19 BPF
        uses: dabao1955/kernel_build_action@main
        with:
          kernel-url: https://github.com/ahmedhanbal/kernel_xiaomi_spes/
          kernel-dir: msm-4.19
          kernel-branch: main-a16-sm8250-base-ksufs
          config: vendor/spes-perf_defconfig
          arch: arm64
          aosp-gcc: true
          aosp-clang: true
          ksu-other: false
          ksu-url: https://github.com/Youffx/KernelSU-Next
          aosp-clang-version: r584948
          anykernel3: true
          anykernel3-url: https://github.com/ahmedhanbal/AnyKernel
          extra-make-args: "LLVM=1" "LLVM_IAS=1" "LOCALVERSION=-susNext"
